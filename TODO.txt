- Kind of just.. Re write the includes and make them readable
- Add subtraction

- Modify structure, allow negative numbers to exist
- Modify ADD() to allow negative number processing
- Add SUBTRACT() to handle subtraction and modify for negative number processing

ADD() and SUBTRACT() may call each other depending on how numbers are processed
